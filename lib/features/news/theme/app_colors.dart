import 'package:flutter/material.dart';

class NewsColors {
  NewsColors._();

  // خلفيات بيضاء ونظيفة
  static const Color background = Colors.white;
  static const Color surface = Color(0xFFF9F9F9);
  static const Color cardBackground = Colors.white;

  // نصوص
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF8E8E93);
  static const Color textHint = Color(0xFFC7C7CC);

  // حدود وفواصل
  static const Color border = Color(0xFFE5E5EA);
  static const Color divider = Color(0xFFF2F2F7);

  // ألوان الإجراءات
  static const Color likeActive = Color(0xFFFF3B30);
  static const Color upvoteActive = Color(0xFFFF9500);
  static const Color saveActive = Color(0xFFFFD60A);
  static const Color accent = Color(0xFF007AFF);
  static const Color repost = Color(0xFF34C759);
}
