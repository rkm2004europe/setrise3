class RizeModerationService {
  Future<void> reportPost(String postId) async {
    await Future.delayed(const Duration(seconds: 1));
  }

  Future<void> deletePost(String postId) async {
    await Future.delayed(const Duration(seconds: 1));
  }
}
