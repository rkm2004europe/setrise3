import 'package:flutter/material.dart';

class DateColors {
  DateColors._();
  static const Color bg = Color(0xFF0D0D0D);
  static const Color surface = Color(0xFF1C1C1E);
  static const Color text = Colors.white;
  static const Color text2 = Color(0xFF8E8E93);
  static const Color accent = Color(0xFFFF2D55); // أحمر وردي
  static const Color like = Color(0xFF34C759);    // أخضر
  static const Color nope = Color(0xFFFF3B30);    // أحمر
  static const Color superLike = Color(0xFF007AFF); // أزرق
  static const Color border = Color(0xFF38383A);
}
