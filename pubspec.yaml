# core/pubspec.yaml

name: core
description: Core package
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  shared_preferences: ^2.2.0
  connectivity_plus: ^5.0.0
  get_it: ^7.6.0
